
# redis配置文件示例
# Redis configuration file example.
#
# 注意为了读取配置文件的顺序，redis必须使用文件路径作为第一个参数
# Note that in order to read the configuration file, Redis must be
# started with the file path as first argument:
#
# ./redis-server /path/to/redis.conf

# 我自己的翻译：单位上的注意点：当需要内存大小的时候，可能需要用通常的格式是1k 5GB 4M等等指定它。
# 有道翻译：关于单元的注意事项：当需要内存大小时，可以指定为1k 5GB 4M等通常形式
# Note on units: when memory size is needed, it is possible to specify
# it in the usual form of 1k 5GB 4M and so forth:
#
# 1k => 1000 bytes
# 1kb => 1024 bytes
# 1m => 1000000 bytes
# 1mb => 1024*1024 bytes
# 1g => 1000000000 bytes
# 1gb => 1024*1024*1024 bytes
# 单位是大小写不敏感的，所以 1GB 1Gb 1gB都是一样的。
# units are case insensitive so 1GB 1Gb 1gB are all the same.

################################## INCLUDES ###################################

# 这里包含一个或更多的配置文件。
# 在这里包含一个或多个其他配置文件。这是有用的，
# 如果你有一个标准的模板匹配所有的Redis服务器，但也需要自定义一些不同服务器的设置。Include文件指令可以包含其他文件，要明智地使用它
# Include one or more other config files here.  This is useful if you
# have a standard template that goes to all Redis servers but also need
# to customize a few per-server settings.  Include files can include
# other files, so use this wisely.
#
# 注意include指令不会被admin或Redis Sentinel的命令“CONFIG REWRITE”重写。
# 因为Redis总是使用最新处理的行作为配置指令的值，你最好把include放在这个文件的开头，以避免在运行时覆盖配置更改。
# 如果您对使用include来覆盖配置选项感兴趣，那么最好将include作为最后一行。
# Notice option "include" won't be rewritten by command "CONFIG REWRITE"
# from admin or Redis Sentinel. Since Redis always uses the last processed
# line as value of a configuration directive, you'd better put includes
# at the beginning of this file to avoid overwriting config change at runtime.
#
# If instead you are interested in using includes to override configuration
# options, it is better to use include as the last line.
#
# include /path/to/local.conf
# include /path/to/other.conf

################################## NETWORK #####################################

# By default, if no "bind" configuration directive is specified, Redis listens
# for connections from all the network interfaces available on the server.
# It is possible to listen to just one or multiple selected interfaces using
# the "bind" configuration directive, followed by one or more IP addresses.
# 默认情况下，如果没有指定“bind”配置指令，Redis将监听来自服务器上所有可用网络接口的连接。
# 使用“bind”配置指令，可以只监听一个或多个选定的接口，后面跟着一个或多个IP地址
#
# Examples:
#
# bind 192.168.1.100 10.0.0.1
# bind 127.0.0.1 ::1
# 按照源码来看，如果多加行，那么后面一行会把前面一行替代
#
# 如果运行Redis的计算机直接暴露在绑定到所有接口是危险的，并且会将实例暴露给互联网上的每个人。
#因此，默认情况下，我们取消注释下面的bind指令，
#这将强制Redis只监听IPv4回看接口地址（这意味着Redis将只能接受来自运行到同一台计算机的客户端的连接）。
# ~~~ WARNING ~~~ If the computer running Redis is directly exposed to the
# internet, binding to all the interfaces is dangerous and will expose the
# instance to everybody on the internet. So by default we uncomment the
# following bind directive, that will force Redis to listen only into
# the IPv4 lookback interface address (this means Redis will be able to
# accept connections only from clients running into the same computer it
# is running).
#
# IF YOU ARE SURE YOU WANT YOUR INSTANCE TO LISTEN TO ALL THE INTERFACES
# JUST COMMENT THE FOLLOWING LINE.
# ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
bind 127.0.0.1

# 保护模式是一层安全保护，以避免互联网上开放的Redis实例被访问和利用。
# Protected mode is a layer of security protection, in order to avoid that
# Redis instances left open on the internet are accessed and exploited.
#
# When protected mode is on and if:
# 当 protected 模式启用且如果以下两个条件成立 

# 1) The server is not binding explicitly to a set of addresses using the
#    "bind" directive. 服务没有通过bind指令显示绑定地址
# 2) No password is configured. 没有配置密码
#
# 服务只接受127.0.0.1的链接
# The server only accepts connections from clients connecting from the
# IPv4 and IPv6 loopback addresses 127.0.0.1 and ::1, and from Unix domain
# sockets.
#
# 缺省情况下，启用保护模式。只有当你确定你想要其他主机的客户端连接到Redis时，即使没有配置身份验证，
# ，也没有使用“bind”指令显式列出一组特定的接口，你才应该禁用它
# By default protected mode is enabled. You should disable it only if
# you are sure you want clients from other hosts to connect to Redis
# even if no authentication is configured, nor a specific set of interfaces
# are explicitly listed using the "bind" directive.
protected-mode yes

# 在指定的端口上接受连接，默认是6379
# Accept connections on the specified port, default is 6379 (IANA #815344).
# If port 0 is specified Redis will not listen on a TCP socket.
port 6369

# tcp 监听 待办事项列表
# TCP listen() backlog.
#
# 请注意，Linux内核将静默地将其截断为/proc/sys/net/core/somaxconn的值，
# 因此请确保提高somaxconn和tcp_max_syn_backlog的值，以获得所需的效果
# 在每秒请求数高的环境中，您需要高的积压，以避免缓慢的客户端连接问题
# In high requests-per-second environments you need an high backlog in order
# to avoid slow clients connections issues. Note that the Linux kernel
# will silently truncate it to the value of /proc/sys/net/core/somaxconn so
# make sure to raise both the value of somaxconn and tcp_max_syn_backlog
# in order to get the desired effect.
tcp-backlog 511

# Unix socket.
#
# Specify the path for the Unix socket that will be used to listen for
# incoming connections. There is no default, so Redis will not listen
# on a unix socket when not specified.
#
# unixsocket /tmp/redis.sock
# unixsocketperm 700

# 在客户端N秒空闲之后关闭连接
# Close the connection after a client is idle for N seconds (0 to disable)
timeout 0

# TCP keepalive.
#
# 如果非0，使用SO_KEEPALIVE在没有通信的情况下向客户端发送TCP ack。这有两个有用的原因
# If non-zero, use SO_KEEPALIVE to send TCP ACKs to clients in absence
# of communication. This is useful for two reasons:
#
# 1) Detect dead peers. 检测失效对等体
# 2) Take the connection alive from the point of view of network
#    equipment in the middle. 从中间的网络设备的角度来看，连接是活的
# 在Linux操作系统中，指定的值（单位为秒）是发送ack的周期
# 请注意，要关闭连接需要两倍的时间
# 在其他内核上，周期取决于内核配置
# On Linux, the specified value (in seconds) is the period used to send ACKs.
# Note that to close the connection the double of the time is needed.
# On other kernels the period depends on the kernel configuration.
#
#  https://developer.aliyun.com/article/757742
# 该选项的合理值是300秒
# A reasonable value for this option is 300 seconds, which is the new
# Redis default starting with Redis 3.2.1.
tcp-keepalive 300

################################# GENERAL #####################################

# By default Redis does not run as a daemon. Use 'yes' if you need it.
# Note that Redis will write a pid file in /var/run/redis.pid when daemonized.
daemonize no

# 如果你从upstart或systemd运行Redis， Redis可以与你的监督树交互
# If you run Redis from upstart or systemd, Redis can interact with your
# supervision tree. Options:
#   supervised no      - no supervision interaction
#   supervised upstart - signal upstart by putting Redis into SIGSTOP mode
#   supervised systemd - signal systemd by writing READY=1 to $NOTIFY_SOCKET
#   supervised auto    - detect upstart or systemd method based on
#                        UPSTART_JOB or NOTIFY_SOCKET environment variables
# Note: these supervision methods only signal "process is ready."
#       They do not enable continuous liveness pings back to your supervisor.
# 这些监督方法只表示“进程准备好了”。它们不能持续地向你的主管发送动态信息
supervised no

# If a pid file is specified, Redis writes it where specified at startup
# and removes it at exit.
# 如果一个pid文件被指定，Redis启动的时候会写他，退出的时候会移除。
#
# When the server runs non daemonized, no pid file is created if none is
# specified in the configuration. When the server is daemonized, the pid file
# is used even if not specified, defaulting to "/var/run/redis.pid".
# 当服务器运行在非守护进程状态时，如果配置中没有指定pid文件，则不会创建pid文件
# 当服务器被daemon化时，即使没有指定pid文件也会被使用，默认为“/var/run/redis.pid”。
#
# Creating a pid file is best effort: if Redis is not able to create it
# nothing bad happens, the server will start and run normally.
# 如果Redis不能创建它，也不会有什么不好的事情发生，服务器将启动并正常运行。
pidfile /var/run/redis_6379.pid

# Specify the server verbosity level.
# 指定服务器的verbosity 级别
# This can be one of:
# debug (a lot of information, useful for development/testing)
# 调试级别（大量信息，有助于开发和测试）
# verbose (many rarely useful info, but not a mess like the debug level)
# notice (moderately verbose, what you want in production probably)
# warning (only very important / critical messages are logged)
loglevel notice

# Specify the log file name. Also the empty string can be used to force
# Redis to log on the standard output. Note that if you use standard
# output for logging but daemonize, logs will be sent to /dev/null
logfile ""

# To enable logging to the system logger, just set 'syslog-enabled' to yes,
# and optionally update the other syslog parameters to suit your needs.
# syslog-enabled no

# Specify the syslog identity.
# syslog-ident redis

# Specify the syslog facility. Must be USER or between LOCAL0-LOCAL7.
# syslog-facility local0

# 指定数据库的数量
# 默认数据库是db0，您可以使用select <dbid>在每个连接的基础上选择一个不同的数据库，
# 其中dbid是一个介于0和 'databases'-1 之间的数字
# Set the number of databases. The default database is DB 0, you can select
# a different one on a per-connection basis using SELECT <dbid> where
# dbid is a number between 0 and 'databases'-1
databases 16

################################ SNAPSHOTTING  ################################
#
# Save the DB on disk:
#
#   save <seconds> <changes>
#
#   如果给定的秒数和给定的对DB的写操作数都发生了，将保存DB
#   Will save the DB if both the given number of seconds and the given
#   number of write operations against the DB occurred.
#
#   在下面的示例中，有以下三种保存行为
#   In the example below the behaviour will be to save:
#   15分钟以后至少有1个key变化
#   after 900 sec (15 min) if at least 1 key changed
#   5分钟后至少10个key变化
#   after 300 sec (5 min) if at least 10 keys changed
#   60秒后至少10000个key变化
#   after 60 sec if at least 10000 keys changed
#   注意：你可以通过注释所有save行来完全禁用
#   Note: you can disable saving completely by commenting out all "save" lines.
#   还可以通过添加带有单个空字符串参数的保存指令来删除所有先前配置的保存点，如下面的示例所示
#   It is also possible to remove all the previously configured save
#   points by adding a save directive with a single empty string argument
#   like in the following example:
#
#   save ""

save 900 1
save 300 10
save 60 10000

# 默认情况下 如果rdb镜像启用(至少一个写入点)且最新一次后台保存失败 那么 redis会停止接受写入 
# By default Redis will stop accepting writes if RDB snapshots are enabled
# (at least one save point) and the latest background save failed.

# 这个会导致 这将使用户意识到（以一种困难的方式）数据没有正确地保存在磁盘上，
# 否则很可能没有人会注意到，并且会发生一些灾难（就是说不停止写入的话）
# This will make the user aware (in a hard way) that data is not persisting
# on disk properly, otherwise chances are that no one will notice and some
# disaster will happen.
# 如果后台保存进程又开始工作了 ，那么redis会自动允许再次写入
# If the background saving process will start working again Redis will
# automatically allow writes again.
#
# 然而，如果你设置了适当的Redis服务器和持久化监控，你可能想要禁用这个功能，
# 这样Redis即使在磁盘、权限等方面出现问题也能继续正常工作。
# However if you have setup your proper monitoring of the Redis server
# and persistence, you may want to disable this feature so that Redis will
# continue to work as usual even if there are problems with disk,
# permissions, and so forth.
stop-writes-on-bgsave-error yes

# 当dump rdb的时候使用LZF算法压缩字符串对象吗？
# 如果你想在子进程保存的时候节省点cpu就设置为no
# 但是如果有可压缩的值或key的话数据集会变大
# Compress string objects using LZF when dump .rdb databases?
# For default that's set to 'yes' as it's almost always a win.
# If you want to save some CPU in the saving child set it to 'no' but
# the dataset will likely be bigger if you have compressible values or keys.
rdbcompression yes

# 从rdb5开始crc64 checksum 会放在文件的最后
# 自RDB版本5以来，CRC64校验和被放置在文件的末尾。
# 这使得格式更能抵抗损坏，但在保存和加载RDB文件时，会有性能损失（大约10%），因此您可以禁用它以获得最大性能
# Since version 5 of RDB a CRC64 checksum is placed at the end of the file.
# This makes the format more resistant to corruption but there is a performance
# hit to pay (around 10%) when saving and loading RDB files, so you can disable it
# for maximum performances.
#
# RDB files created with checksum disabled have a checksum of zero that will
# tell the loading code to skip the check.
rdbchecksum yes

# The filename where to dump the DB
dbfilename dump.rdb

# The working directory.
#
# The DB will be written inside this directory, with the filename specified
# above using the 'dbfilename' configuration directive.
#
# AOF文件也会在这个文件夹中创建
# The Append Only File will also be created inside this directory.
#
# Note that you must specify a directory here, not a file name.
dir ./

################################# REPLICATION #################################

# 主从复制。使用slaveof 命令 使得一个redis实例 变成另外一个redis实例的复制。
# 关于Redis复制，有几件事需要尽快理解
# Master-Slave replication. Use slaveof to make a Redis instance a copy of
# another Redis server. A few things to understand ASAP about Redis replication.
#
# 1）redis复制是异步的，但是你可以配置一个master 如果和一些slave不连接的话就 停止接收写入
# 但是，您可以将主服务器配置为，如果它看起来没有与至少给定数量的从服务器连接，则停止接受写操作
# 1) Redis replication is asynchronous, but you can configure a master to
#    stop accepting writes if it appears to be not connected with at least
#    a given number of slaves.

# 2）如果复制链路丢失相对较短的时间，Redis从端可以执行与主端的部分重同步
#    您可能希望根据您的需要将复制积压大小（请参阅本文件的下一部分）配置为合理的值
# 2) Redis slaves are able to perform a partial resynchronization with the
#    master if the replication link is lost for a relatively small amount of
#    time. You may want to configure the replication backlog size (see the next
#    sections of this file) with a sensible value depending on your needs.
# 3）复制是自动的，不需要用户干预。在网络分区之后，从服务器会自动尝试重新连接到主服务器，并与主服务器重新同步
# 3) Replication is automatic and does not need user intervention. After a
#    network partition slaves automatically try to reconnect to masters
#    and resynchronize with them.
#
# slaveof <masterip> <masterport>
# 如果主服务器受密码保护（使用下面的“requirepass”配置指令），
# 则可以在启动复制同步进程之前告诉从服务器进行身份验证，否则主服务器将拒绝从服务器的请求
# If the master is password protected (using the "requirepass" configuration
# directive below) it is possible to tell the slave to authenticate before
# starting the replication synchronization process, otherwise the master will
# refuse the slave request.
#
# masterauth <master-password>

# 当从服务器失去与主服务器的连接时，或者当复制仍在进行时，从服务器可以采取两种不同的方式
# When a slave loses its connection with the master, or when the replication
# is still in progress, the slave can act in two different ways:
#
# 1） 如果slave- server -stale-data设置为‘yes’（默认值），
#    slave仍然会回复客户端请求，可能会有过期的数据，或者如果这是第一次同步，数据集可能只是空的
# 1) if slave-serve-stale-data is set to 'yes' (the default) the slave will
#    still reply to client requests, possibly with out of date data, or the
#    data set may just be empty if this is the first synchronization.
#    如果slave- server -stale-data被设置为no， slave将回复除了INFO和SLAVEOF之外，
#    所有类型的命令都出现了错误“SYNC with master in progress”
# 2) if slave-serve-stale-data is set to 'no' the slave will reply with
#    an error "SYNC with master in progress" to all the kind of commands
#    but to INFO and SLAVEOF.
#  stale是陈旧的意思
slave-serve-stale-data yes 


# https://mp.weixin.qq.com/s/frLDENsPQrv5sZ2PBVzoHg
# 你可以配置一个slave实例是否接受写入
# 对从实例进行写操作可能有助于存储一些临时数据（因为写在从实例上的数据在与主实例重新同步后很容易被删除），
# 但如果客户端由于配置错误而写入从实例，也可能导致问题
# You can configure a slave instance to accept writes or not. Writing against
# a slave instance may be useful to store some ephemeral data (because data
# written on a slave will be easily deleted after resync with the master) but
# may also cause problems if clients are writing to it because of a
# misconfiguration.
# 从redis2.6版本开始salves 默认是只读的。
# Since Redis 2.6 by default slaves are read-only.
# 
# 只读slave不被设计为暴露给互联网上不受信任的客户端。它只是防止滥用实例的保护层。
# 默认情况下，只读从服务器仍然输出所有管理命令，如CONFIG、DEBUG等
# 在一定程度上，您可以使用‘rename-command’来跟踪所有管理/危险命令，从而提高只读从服务器的安全性。
# Note: read only slaves are not designed to be exposed to untrusted clients
# on the internet. It's just a protection layer against misuse of the instance.
# Still a read only slave exports by default all the administrative commands
# such as CONFIG, DEBUG, and so forth. To a limited extent you can improve
# security of read only slaves using 'rename-command' to shadow all the
# administrative / dangerous commands.
slave-read-only yes

# 复制同步策略：磁盘和套接字。
# Replication SYNC strategy: disk or socket.
# 无磁盘复制目前处于实验阶段 到redis6 这个注释才去掉
# -------------------------------------------------------
# WARNING: DISKLESS REPLICATION IS EXPERIMENTAL CURRENTLY
# -------------------------------------------------------
# 如果新的从服务器和重新连接的从服务器无法继续复制过程，则需要执行所谓的“完全同步”。
# RDB文件从主服务器传输到从服务器。传播可以通过两种不同的方式发生
# New slaves and reconnecting slaves that are not able to continue the replication
# process just receiving differences, need to do what is called a "full
# synchronization". An RDB file is transmitted from the master to the slaves.
# The transmission can happen in two different ways:
#
# ） 磁盘支持：Redis主服务器创建一个新进程，将RDB文件写入磁盘。随后，该文件由父进程逐步传输到从进程。
# 1) Disk-backed: The Redis master creates a new process that writes the RDB
#                 file on disk. Later the file is transferred by the parent
#                 process to the slaves incrementally.
#  2)无磁盘（Diskless）： Redis主服务器创建一个新进程，直接将RDB文件写入从服务器套接字，而不涉及磁盘。
# 2) Diskless: The Redis master creates a new process that directly writes the
#              RDB file to slave sockets, without touching the disk at all.
# 使用磁盘支持的复制，在生成RDB文件的同时，只要当前生成RDB文件的子进程完成其工作，就可以为更多的从进程排队并提供RDB文件。
# 而使用无磁盘复制时，一旦传输开始，到达的新从服务器将进入队列，并且在当前传输终止时将开始新的传输
# With disk-backed replication, while the RDB file is generated, more slaves
# can be queued and served with the RDB file as soon as the current child producing
# the RDB file finishes its work. With diskless replication instead once
# the transfer starts, new slaves arriving will be queued and a new transfer
# will start when the current one terminates.
#
# 当使用无磁盘复制时，主服务器在开始传输之前等待一段可配置的时间（以秒为单位），希望有多个从服务器到达，并且传输可以并行化。
# When diskless replication is used, the master waits a configurable amount of
# time (in seconds) before starting the transfer in the hope that multiple slaves
# will arrive and the transfer can be parallelized.
#
# 对于慢速磁盘和快速（大带宽）网络，无磁盘复制效果更好
# With slow disks and fast (large bandwidth) networks, diskless replication
# works better.
repl-diskless-sync no

# 当启用无磁盘复制时，可以配置服务器等待的延迟，以便生成通过套接字将RDB传输到从服务器的子服务器
# When diskless replication is enabled, it is possible to configure the delay
# the server waits in order to spawn the child that transfers the RDB via socket
# to the slaves.
#
# 这一点很重要，因为一旦传输开始，就不可能为到达的新slave提供服务，
# 它们将排队等待下一个RDB传输，因此服务器等待延迟，以便让更多的slave到达
# This is important since once the transfer starts, it is not possible to serve
# new slaves arriving, that will be queued for the next RDB transfer, so the server
# waits a delay in order to let more slaves arrive.
#
# 延迟时间以秒为单位，缺省值为5秒。要完全禁用它，只需将其设置为0秒，传输将尽快开始
# The delay is specified in seconds, and by default is 5 seconds. To disable
# it entirely just set it to 0 seconds and the transfer will start ASAP.
repl-diskless-sync-delay 5

# 从服务器以预定义的时间间隔向服务器发送ping。可以使用repl_ping_slave_period选项更改此间隔。缺省值是10秒
# Slaves send PINGs to server in a predefined interval. It's possible to change
# this interval with the repl_ping_slave_period option. The default value is 10
# seconds.
#
# repl-ping-slave-period 10

# 以下选项设置的复制超时
# The following option sets the replication timeout for:
# 
# 1） 从slave的角度来看，同步期间的批量传输I/O
# 1) Bulk transfer I/O during SYNC, from the point of view of slave.
# 2）从 slave（数据、ping）的角度来看，master超时。
# 2) Master timeout from the point of view of slaves (data, pings).
# 3) 从主机的角度看 slave超时 REPLCONF ACK pings
# 3) Slave timeout from the point of view of masters (REPLCONF ACK pings).
#
# 重要的是要确保这个值大于为repl-ping-slave-period指定的值，
# 否则每次主服务器和从服务器之间的流量较低时都会检测到超时
# It is important to make sure that this value is greater than the value
# specified for repl-ping-slave-period otherwise a timeout will be detected
# every time there is low traffic between the master and the slave.
#
# repl-timeout 60
# 在SYNC之后禁用TCP_NODELAY吗？
# Disable TCP_NODELAY on the slave socket after SYNC?
# 如果你选择“是”，Redis将使用更少的TCP数据包和更少的带宽向slave发送数据
# 但是这会增加数据出现在从属端的延迟，对于使用默认配置的Linux内核，延迟可达40毫秒
# If you select "yes" Redis will use a smaller number of TCP packets and
# less bandwidth to send data to slaves. But this can add a delay for
# the data to appear on the slave side, up to 40 milliseconds with
# Linux kernels using a default configuration.
#
# 如果选择“no”，则从端出现数据的延迟将减少，但将使用更多带宽用于复制
# If you select "no" the delay for data to appear on the slave side will
# be reduced but more bandwidth will be used for replication.
#
# 默认情况下，我们针对低延迟进行了优化，但在非常高的流量条件下，
# 或者当主服务器和从服务器相距许多hops时，将此设置为“yes”可能是一个好主意
# By default we optimize for low latency, but in very high traffic conditions
# or when the master and slaves are many hops away, turning this to "yes" may
# be a good idea.
repl-disable-tcp-nodelay no

# 设置复制积压大小。积压是一个缓冲区，用于在从服务器断开连接一段时间后积累从服务器数据，
# 因此当从服务器想要再次重新连接时，通常不需要完全重新同步，但部分重新同步就足够了，只需传递从服务器断开连接时丢失的部分数据。
# Set the replication backlog size. The backlog is a buffer that accumulates
# slave data when slaves are disconnected for some time, so that when a slave
# wants to reconnect again, often a full resync is not needed, but a partial
# resync is enough, just passing the portion of data the slave missed while
# disconnected.
#
# 复制积压越大，从服务器断开连接的时间就越长，以后就能够执行部分重新同步
# The bigger the replication backlog, the longer the time the slave can be
# disconnected and later be able to perform a partial resynchronization.
#
# 只有在至少有一个从属连接时才分配积压
# The backlog is only allocated once there is at least a slave connected.
#
# repl-backlog-size 1mb

# 当主服务器在一段时间内不再连接从服务器时，积压将被释放。下面的选项配置了释放积压缓冲区所需的秒数（从最后一个从服务器断开连接开始）
# After a master has no longer connected slaves for some time, the backlog
# will be freed. The following option configures the amount of seconds that
# need to elapse, starting from the time the last slave disconnected, for
# the backlog buffer to be freed.
#
# 0代表不释放backlog
# A value of 0 means to never release the backlog.
#
# repl-backlog-ttl 3600

# 从机优先级是Redis在INFO输出中发布的一个整数。Redis哨兵使用它来选择一个从，当主不再正常工作时，将其提升为主
# The slave priority is an integer number published by Redis in the INFO output.
# It is used by Redis Sentinel in order to select a slave to promote into a
# master if the master is no longer working correctly.
#
# 优先级较低的slave被认为更有利于升级，例如，如果有三个优先级为10、100、25的奴隶，哨兵会选择优先级为10的奴隶，这是最低的
# A slave with a low priority number is considered better for promotion, so
# for instance if there are three slaves with priority 10, 100, 25 Sentinel will
# pick the one with priority 10, that is the lowest.
#
# 然而，一个特殊的优先级为0的从机不能扮演主从机的角色，所以一个优先级为0的从机将永远不会被Redis哨兵选中升级
# However a special priority of 0 marks the slave as not able to perform the
# role of master, so a slave with priority of 0 will never be selected by
# Redis Sentinel for promotion.
#
# By default the priority is 100.
slave-priority 100

# 如果连接的从服务器少于N个，并且延迟小于或等于M秒，主服务器就有可能停止接受写操作
# It is possible for a master to stop accepting writes if there are less than
# N slaves connected, having a lag less or equal than M seconds.
#
# 这N个从服务需要online 状态
# The N slaves need to be in "online" state.
#
# 以秒为单位的延迟（必须<=指定的值）是从从服务器接收到的最后一个ping开始计算的，该ping通常每秒发送一次
# The lag in seconds, that must be <= the specified value, is calculated from
# the last ping received from the slave, that is usually sent every second.
#
# 此选项不能保证N个副本将接受写入，但是在没有足够的slave可用的情况下，会将丢失写入的暴露窗口限制为指定的秒数
# This option does not GUARANTEE that N replicas will accept the write, but
# will limit the window of exposure for lost writes in case not enough slaves
# are available, to the specified number of seconds.
#
# 例如，需要至少3个slave，并且延迟<= 10秒
# For example to require at least 3 slaves with a lag <= 10 seconds use:
#
# min-slaves-to-write 3
# min-slaves-max-lag 10
#
# 将其中一个设置为0将禁用该特性
# Setting one or the other to 0 disables the feature.
#
# By default min-slaves-to-write is set to 0 (feature disabled) and
# min-slaves-max-lag is set to 10.

# Redis主服务器能够以不同的方式列出附属从服务器的地址和端口。例如，“INFO replication”部分提供了这些信息，
# Redis Sentinel在其他工具中使用这些信息来发现从实例。另一个可用此信息的地方是master的“ROLE”命令的输出
# A Redis master is able to list the address and port of the attached
# slaves in different ways. For example the "INFO replication" section
# offers this information, which is used, among other tools, by
# Redis Sentinel in order to discover slave instances.
# Another place where this info is available is in the output of the
# "ROLE" command of a masteer.
#
# 从机通常报告的列出的IP和地址可以通过以下方式获得
# The listed IP and address normally reported by a slave is obtained
# in the following way:
#
#   IP：通过检查对端地址自动检测从端连接到主端的套接字的编号
#   IP: The address is auto detected by checking the peer address
#   of the socket used by the slave to connect with the master.
#   端口：复制握手期间从端通信的端口，通常是从端用来列出连接的端口
#   Port: The port is communicated by the slave during the replication
#   handshake, and is normally the port that the slave is using to
#   list for connections.
#
# 但是，当使用端口转发或网络地址转换（NAT）时，从服务器实际上可以通过不同的IP和端口对访问。
# 从属服务器可以使用以下两个选项向其主服务器报告一组特定的IP和端口，以便INFO和ROLE都将报告这些值
# However when port forwarding or Network Address Translation (NAT) is
# used, the slave may be actually reachable via different IP and port
# pairs. The following two options can be used by a slave in order to
# report to its master a specific set of IP and port, so that both INFO
# and ROLE will report those values.
# 如果您只需要覆盖端口或IP地址，则无需同时使用这两个选项
# There is no need to use both the options if you need to override just
# the port or the IP address.
#
# slave-announce-ip 5.5.5.5
# slave-announce-port 1234

################################## SECURITY ###################################

# 要求客户端在处理任何其他命令之前发出AUTH <PASSWORD>。在您不相信其他人可以访问运行redis-server的主机的环境中，这可能很有用
# Require clients to issue AUTH <PASSWORD> before processing any other
# commands.  This might be useful in environments in which you do not trust
# others with access to the host running redis-server.
#
# 为了向后兼容，这个应该被注释掉，因为大多数人不需要auth（例如，他们运行自己的服务器）。
# This should stay commented out for backward compatibility and because most
# people do not need auth (e.g. they run their own servers).
# 
# 警告：由于Redis非常快，外部用户可以尝试到每秒15万个密码。这意味着你应该使用一个非常强大的密码，否则它将很容易被破解
# Warning: since Redis is pretty fast an outside user can try up to
# 150k passwords per second against a good box. This means that you should
# use a very strong password otherwise it will be very easy to break.
#
# requirepass foobared

# 命令重命名
# Command renaming.
# 在共享环境中，可以更改危险命令的名称。
# 例如，CONFIG命令可能被重命名为难以猜测的东西，以便它仍然可用于内部使用的工具，但不可用于一般客户端
# It is possible to change the name of dangerous commands in a shared
# environment. For instance the CONFIG command may be renamed into something
# hard to guess so that it will still be available for internal-use tools
# but not available for general clients.
#
# Example:
#
# rename-command CONFIG b840fc02d524045429941cc15f59e41cb7be6c52
# 也可以通过将命令重命名为空字符串来完全终止命令
# It is also possible to completely kill a command by renaming it into
# an empty string:
#
# rename-command CONFIG ""
#
# 请注意，更改记录到AOF文件或传输到slave的命令的名称可能会导致问题
# Please note that changing the name of commands that are logged into the
# AOF file or transmitted to slaves may cause problems.

################################### LIMITS ####################################

# 设置同时连接的最大客户端数。默认情况下，这个限制被设置为10000个客户端，但是如果Redis服务器不能配置进程文件限制来允许指定的限制，
# 允许的最大客户端数量被设置为当前文件限制减去32（因为Redis保留了一些文件描述符供内部使用）。
# Set the max number of connected clients at the same time. By default
# this limit is set to 10000 clients, however if the Redis server is not
# able to configure the process file limit to allow for the specified limit
# the max number of allowed clients is set to the current file limit
# minus 32 (as Redis reserves a few file descriptors for internal uses).
#
# 一旦达到限制，Redis将关闭所有新连接，并发送错误“max number of clients reached”。
# Once the limit is reached Redis will close all the new connections sending
# an error 'max number of clients reached'.
#
# maxclients 10000

# 不要使用超过指定字节数的内存。当内存达到限制时，Redis将尝试删除键根据选择的清除策略（参见maxmemory-policy）
# Don't use more memory than the specified amount of bytes.
# When the memory limit is reached Redis will try to remove keys
# according to the eviction policy selected (see maxmemory-policy).
#
# 如果Redis不能根据策略删除键，或者如果策略设置为“noeviction”，
# Redis将开始错误地回复需要更多内存的命令，如set， LPUSH等，并将继续回复只读命令，如GET
# If Redis can't remove keys according to the policy, or if the policy is
# set to 'noeviction', Redis will start to reply with errors to commands
# that would use more memory, like SET, LPUSH, and so on, and will continue
# to reply to read-only commands like GET.
#
# 当使用Redis作为LRU缓存或为实例设置硬内存限制（使用‘noeviction’策略）时，此选项通常很有用。
# This option is usually useful when using Redis as an LRU cache, or to set
# a hard memory limit for an instance (using the 'noeviction' policy).
#
# 警告：如果您将slave连接到一个实例，则需要从已使用的内存计数中减去为slave提供的输出缓冲区的大小，
# 以便网络问题/重新同步不会触发键被驱逐的循环，
# 反过来，slave的输出缓冲区充满了被驱逐的键的del，从而触发删除更多的键，以此类推，直到数据库完全清空
# WARNING: If you have slaves attached to an instance with maxmemory on,
# the size of the output buffers needed to feed the slaves are subtracted
# from the used memory count, so that network problems / resyncs will
# not trigger a loop where keys are evicted, and in turn the output
# buffer of slaves is full with DELs of keys evicted triggering the deletion
# of more keys, and so forth until the database is completely emptied.
#
# 总之……如果您有附属，建议您为maxmemory设置一个较低的限制，
# 以便系统上有一些空闲的RAM用于附属输出缓冲区（但如果策略是‘noeviction’，则不需要这样做）。
# In short... if you have slaves attached it is suggested that you set a lower
# limit for maxmemory so that there is some free RAM on the system for slave
# output buffers (but this is not needed if the policy is 'noeviction').
#
# maxmemory <bytes>

# 最大内存策略： 当达到maxmemory时，Redis将如何选择删除什么。
# 1.使用LRU算法删除带有过期集的键 从已设置过期时间的数据集（server.db[i].expires）中挑选最近最少使用的数据淘汰
# 2.使用lru算法删除任意键
# 3.删除随机的一个过期键
# 4.删除随机的任意一个键
# 5.删除最近过期时间的键
# 6. 不过期，只在写入操作时返回一个错误
# 注意：任何这些策略，如果没有合适的键可以删除，Redis会在写操作时返回一个错误。
# 当写这个注意点的时候，这些命令包含：

# 默认策略是：noeviction
# MAXMEMORY POLICY: how Redis will select what to remove when maxmemory
# is reached. You can select among five behaviors:
# 
# volatile-lru -> remove the key with an expire set using an LRU algorithm
# allkeys-lru -> remove any key according to the LRU algorithm
# volatile-random -> remove a random key with an expire set
# allkeys-random -> remove a random key, any key
# volatile-ttl -> remove the key with the nearest expire time (minor TTL)
# noeviction -> don't expire at all, just return an error on write operations
#
# Note: with any of the above policies, Redis will return an error on write
#       operations, when there are no suitable keys for eviction.
#
#       At the date of writing these commands are: set setnx setex append
#       incr decr rpush lpush rpushx lpushx linsert lset rpoplpush sadd
#       sinter sinterstore sunion sunionstore sdiff sdiffstore zadd zincrby
#       zunionstore zinterstore hset hsetnx hmset hincrby incrby decrby
#       getset mset msetnx exec sort
#
# The default is:
#
# maxmemory-policy noeviction

# LRU和最小TTL算法不是精确算法，而是近似算法（为了节省内存），因此您可以对其进行调优以提高速度或准确性。
# 默认情况下，Redis会检查五个键，并选择最近使用较少的键，你可以使用以下配置指令更改样本大小
# LRU and minimal TTL algorithms are not precise algorithms but approximated
# algorithms (in order to save memory), so you can tune it for speed or
# accuracy. For default Redis will check five keys and pick the one that was
# used less recently, you can change the sample size using the following
# configuration directive.
#
# 默认值5可以产生足够好的结果。10非常接近真实的LRU，但需要更多的CPU。3是非常快，但不是很准确
# The default of 5 produces good enough results. 10 Approximates very closely
# true LRU but costs a bit more CPU. 3 is very fast but not very accurate.
#
# maxmemory-samples 5

############################## APPEND ONLY MODE ###############################

# 默认情况下，Redis将数据集异步转储到磁盘上。
# 这种模式在许多应用程序中都足够好，但是Redis进程的问题或停电可能会导致几分钟的写丢失（取决于配置的保存点）。
# By default Redis asynchronously dumps the dataset on disk. This mode is
# good enough in many applications, but an issue with the Redis process or
# a power outage may result into a few minutes of writes lost (depending on
# the configured save points).
#
# 只追加文件是一种可选的持久性模式，它提供了更好的持久性。例如，使用默认的数据同步策略（见后面的配置文件），Redis可以在一个戏剧性的事件中只丢失一秒钟的写，
# 比如服务器停电，或者如果Redis进程本身发生了错误，只丢失一次写，但操作系统仍然正常运行
# The Append Only File is an alternative persistence mode that provides
# much better durability. For instance using the default data fsync policy
# (see later in the config file) Redis can lose just one second of writes in a
# dramatic event like a server power outage, or a single write if something
# wrong with the Redis process itself happens, but the operating system is
# still running correctly.
#
# 可以同时启用AOF和RDB持久性而不会出现问题。如果在启动时启用AOF， Redis将加载AOF，这是具有更好的持久性保证的文件
# AOF and RDB persistence can be enabled at the same time without problems.
# If the AOF is enabled on startup Redis will load the AOF, that is the file
# with the better durability guarantees.
#
# Please check http://redis.io/topics/persistence for more information.

appendonly yes

# The name of the append only file (default: "appendonly.aof")

appendfilename "appendonly.aof"

# fsync（）调用告诉操作系统实际将数据写入磁盘，
# 而不是在输出缓冲区中等待更多数据。有些操作系统真的会刷新磁盘上的数据，有些操作系统只是尝试尽快刷新
# 有些操作系统真的会刷新磁盘上的数据，有些操作系统只是尝试尽快刷新。
# The fsync() call tells the Operating System to actually write data on disk
# instead of waiting for more data in the output buffer. Some OS will really flush
# data on disk, some other OS will just try to do it ASAP.
#
# redis支持三种不同的模式
# Redis supports three different modes:
#
# no: 不fsync，让操作系统在需要的时候刷新数据。更快。
# always:每次写入aof后,最安全 。缓慢的,安全的。
# Everysec：每秒只同步一次。 妥协
# no: don't fsync, just let the OS flush the data when it wants. Faster.
# always: fsync after every write to the append only log. Slow, Safest.
# everysec: fsync only one time every second. Compromise.
#
# 默认值是“每秒钟”，因为这通常是速度和数据安全之间的正确折衷。
# 这取决于您是否可以将其放宽为“no”，以便操作系统在需要时刷新输出缓冲区，以获得更好的性能
# （但如果您可以接受一些数据丢失的想法，请考虑默认的持久性模式，即快照），或者相反，使用“always”，这非常慢，但比everysec更安全
# The default is "everysec", as that's usually the right compromise between
# speed and data safety. It's up to you to understand if you can relax this to
# "no" that will let the operating system flush the output buffer when
# it wants, for better performances (but if you can live with the idea of
# some data loss consider the default persistence mode that's snapshotting),
# or on the contrary, use "always" that's very slow but a bit safer than
# everysec.
#
# More details please check the following article:
# http://antirez.com/post/redis-persistence-demystified.html
#
# 不确定就是用everysec.
# If unsure, use "everysec".

# appendfsync always
appendfsync everysec
# appendfsync no

# 当AOF fsync策略设置为always或everysec时，后台保存进程（后台保存或AOF日志后台重写）对磁盘执行大量I/O，在某些Linux配置中，
# Redis可能会在fsync（）调用上阻塞太长时间。
# 请注意，目前还没有解决这个问题，因为即使在不同的线程中执行fsync也会阻塞我们的同步写(2)调用
# When the AOF fsync policy is set to always or everysec, and a background
# saving process (a background save or AOF log background rewriting) is
# performing a lot of I/O against the disk, in some Linux configurations
# Redis may block too long on the fsync() call. Note that there is no fix for
# this currently, as even performing fsync in a different thread will block
# our synchronous write(2) call.
#
# 为了缓解这个问题，可以使用以下选项来防止BGSAVE或BGREWRITEAOF正在进行中的时候fsync（）在主进程中被调用
# In order to mitigate this problem it's possible to use the following option
# that will prevent fsync() from being called in the main process while a
# BGSAVE or BGREWRITEAOF is in progress.
#
# 这意味着当另一个子线程正在保存时，Redis的持久性与“appendfsync none”相同。
# 实际上，这意味着在最坏的情况下（使用默认的Linux设置）可能会丢失长达30秒的日志记录。
# This means that while another child is saving, the durability of Redis is
# the same as "appendfsync none". In practical terms, this means that it is
# possible to lose up to 30 seconds of log in the worst scenario (with the
# default Linux settings).
#
# 如果您有延迟问题，请将此选项改为“yes”。否则，从耐用性的角度来看，这是最安全的选择
# If you have latency problems turn this to "yes". Otherwise leave it as
# "no" that is the safest pick from the point of view of durability.

no-appendfsync-on-rewrite no

# 自动重写只追加的文件。当AOF日志大小增长到指定的百分比时，Redis能够自动隐式地调用BGREWRITEAOF重写日志文件
# Automatic rewrite of the append only file.
# Redis is able to automatically rewrite the log file implicitly calling
# BGREWRITEAOF when the AOF log size grows by the specified percentage.
#
# 它是这样工作的：Redis在最近一次重写后记住AOF文件的大小（如果重启后没有发生重写，则使用启动时的AOF大小）。
# This is how it works: Redis remembers the size of the AOF file after the
# latest rewrite (if no rewrite has happened since the restart, the size of
# the AOF at startup is used).
#
# 将此基本尺寸与当前尺寸进行比较。如果当前大小大于指定的百分比，则触发重写。
# 您还需要指定要重写的AOF文件的最小大小，这对于避免重写AOF文件很有用，即使达到了百分比增加，但它仍然很小
# This base size is compared to the current size. If the current size is
# bigger than the specified percentage, the rewrite is triggered. Also
# you need to specify a minimal size for the AOF file to be rewritten, this
# is useful to avoid rewriting the AOF file even if the percentage increase
# is reached but it is still pretty small.
#
# 指定一个零的百分比，以便禁用自动AOF重写功能
# Specify a percentage of zero in order to disable the automatic AOF
# rewrite feature.

auto-aof-rewrite-percentage 100
auto-aof-rewrite-min-size 64mb

# 在Redis启动过程结束时，当AOF数据被加载回内存时，可能会发现AOF文件被截断。当Redis运行的系统崩溃时，这可能会发生，
# 特别是当ext4文件系统挂载而没有data=ordered选项时（然而，当Redis本身崩溃或中止但操作系统仍然正常工作时，这不会发生）。
# An AOF file may be found to be truncated at the end during the Redis
# startup process, when the AOF data gets loaded back into memory.
# This may happen when the system where Redis is running
# crashes, especially when an ext4 filesystem is mounted without the
# data=ordered option (however this can't happen when Redis itself
# crashes or aborts but the operating system still works correctly).
#
# 当这种情况发生时，Redis可以退出并出现错误，
# 或者加载尽可能多的数据（现在是默认的），如果发现AOF文件在最后被截断，则启动。以下选项控制此行为
# Redis can either exit with an error when this happens, or load as much
# data as possible (the default now) and start if the AOF file is found
# to be truncated at the end. The following option controls this behavior.
#
# 如果AOF -load-truncated设置为yes，则加载一个被截断的AOF文件，并且Redis服务器开始发出日志以通知用户该事件。否则，如果将该选项设置为no，则服务器将以错误终止并拒绝启动。
# 当该选项设置为no时，用户需要在重新启动服务器之前使用“redis-check-aof”实用程序修复AOF文件
# If aof-load-truncated is set to yes, a truncated AOF file is loaded and
# the Redis server starts emitting a log to inform the user of the event.
# Otherwise if the option is set to no, the server aborts with an error
# and refuses to start. When the option is set to no, the user requires
# to fix the AOF file using the "redis-check-aof" utility before to restart
# the server.
#
# 注意，如果发现AOF文件在中间被损坏，服务器仍然会退出并显示错误。此选项仅适用于Redis将尝试从AOF文件读取更多数据，但没有找到足够的字节时
# Note that if the AOF file will be found to be corrupted in the middle
# the server will still exit with an error. This option only applies when
# Redis will try to read more data from the AOF file but not enough bytes
# will be found.
aof-load-truncated yes

################################ LUA SCRIPTING  ###############################

# lua脚本执行的最多时间（毫秒）
# Max execution time of a Lua script in milliseconds.
#
# 如果达到最大执行时间，Redis将记录脚本在最大允许时间后仍在执行，并将开始回复带有错误的查询
# If the maximum execution time is reached Redis will log that a script is
# still in execution after the maximum allowed time and will start to
# reply to queries with an error.
#
# 当长时间运行的脚本超过最大执行时间时，只有script KILL和SHUTDOWN NOSAVE命令可用。第一个可用于停止尚未调用写命令的脚本。
# 第二种方法是在脚本已经发出写命令，
# 但用户不想等待脚本自然终止的情况下关闭服务器的唯一方法
# When a long running script exceeds the maximum execution time only the
# SCRIPT KILL and SHUTDOWN NOSAVE commands are available. The first can be
# used to stop a script that did not yet called write commands. The second
# is the only way to shut down the server in the case a write command was
# already issued by the script but the user doesn't want to wait for the natural
# termination of the script.
#
# 将其设置为0或负值，以便无限制地执行而不发出警告
# Set it to 0 or a negative value for unlimited execution without warnings.
lua-time-limit 5000

################################ REDIS CLUSTER  ###############################
# 警告实验：Redis集群被认为是稳定的代码，但是为了将其标记为“成熟”，我们需要等待一定比例的用户将其部署到生产环境中
# ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
# WARNING EXPERIMENTAL: Redis Cluster is considered to be stable code, however
# in order to mark it as "mature" we need to wait for a non trivial percentage
# of users to deploy it in production.
# ++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
# 正常的Redis实例不能成为Redis集群的一部分；只有作为集群节点启动的节点才可以。
# 为了启动一个Redis实例作为集群节点，启用集群支持取消以下注释
# Normal Redis instances can't be part of a Redis Cluster; only nodes that are
# started as cluster nodes can. In order to start a Redis instance as a
# cluster node enable the cluster support uncommenting the following:
#
# cluster-enabled yes

# 每个集群节点都有一个集群配置文件。这个文件不是
# 适于手工编辑的。它由Redis节点创建和更新。
# 每个Redis集群节点需要一个不同的集群配置文件。确保在同一系统中运行的实例没有重叠的集群配置文件名
# Every cluster node has a cluster configuration file. This file is not
# intended to be edited by hand. It is created and updated by Redis nodes.
# Every Redis Cluster node requires a different cluster configuration file.
# Make sure that instances running in the same system do not have
# overlapping cluster configuration file names.
#
# cluster-config-file nodes-6379.conf

# 集群节点超时是节点不可达的毫秒数，它必须被认为处于故障状态。大多数其他内部时间限制是节点超时的倍数
# Cluster node timeout is the amount of milliseconds a node must be unreachable
# for it to be considered in failure state.
# Most other internal time limits are multiple of the node timeout.
#
# cluster-node-timeout 15000

# 如果主服务器的从服务器的数据看起来太旧，它将避免启动故障转移
# A slave of a failing master will avoid to start a failover if its data
# looks too old.
# 没有一种简单的方法可以让slave实际精确地测量其“数据年龄”，因此执行以下两个检查
# There is no simple way for a slave to actually have a exact measure of
# its "data age", so the following two checks are performed:
#
# 1)如果有多个能够进行故障转移的从服务器，它们会交换消息，以便尝试为具有最佳复制偏移量的从服务器提供优势
#  （处理来自主服务器的更多数据）。
#   从服务器将尝试通过偏移量获得它们的等级，并在故障转移开始时应用与它们的等级成比例的延迟
# 1) If there are multiple slaves able to failover, they exchange messages
#    in order to try to give an advantage to the slave with the best
#    replication offset (more data from the master processed).
#    Slaves will try to get their rank by offset, and apply to the start
#    of the failover a delay proportional to their rank.
#
# 2）每个从服务器都计算最后一次交互的时间
#     它的主人。这可以是收到的最后一个ping或命令（如果主服务器仍处于“已连接”状态），
#    也可以是与主服务器断开连接后经过的时间（如果复制链接当前已断开）。如果最后一个交互太旧，从服务器根本不会尝试故障转移
# 2) Every single slave computes the time of the last interaction with
#    its master. This can be the last ping or command received (if the master
#    is still in the "connected" state), or the time that elapsed since the
#    disconnection with the master (if the replication link is currently down).
#    If the last interaction is too old, the slave will not try to failover
#    at all.
#
# 第2点可由用户自行调整。特别是，如果从服务器与主服务器的最后一次交互后，经过的时间大于
# The point "2" can be tuned by user. Specifically a slave will not perform
# the failover if, since the last interaction with the master, the time
# elapsed is greater than:
#
#   (node-timeout * slave-validity-factor) + repl-ping-slave-period
#
# 例如，如果node-timeout为30秒，slave-validity因子为10，并且假设默认的repl-ping-slave周期为10秒，
# 那么如果从服务器无法与主服务器通信超过310秒，则从服务器将不会尝试进行故障转移
# So for example if node-timeout is 30 seconds, and the slave-validity-factor
# is 10, and assuming a default repl-ping-slave-period of 10 seconds, the
# slave will not try to failover if it was not able to talk with the master
# for longer than 310 seconds.
#
# 较大的slave有效性因子可能允许数据太旧的slave进行主故障转移，而过小的值可能使集群根本无法选择slave
# A large slave-validity-factor may allow slaves with too old data to failover
# a master, while a too small value may prevent the cluster from being able to
# elect a slave at all.
#
# 为了获得最大的可用性，可以将slave-validity-factor设置为0，这意味着，无论它们最后一次与主服务器交互是在什么时候，
# 从服务器将始终尝试对主服务器进行故障转移。（然而，他们总是会尝试使用与他们的偏移等级成比例的延迟）
# For maximum availability, it is possible to set the slave-validity-factor
# to a value of 0, which means, that slaves will always try to failover the
# master regardless of the last time they interacted with the master.
# (However they'll always try to apply a delay proportional to their
# offset rank).
#
# 零是唯一能够保证当所有分区愈合时集群始终能够继续运行的值
# Zero is the only value able to guarantee that when all the partitions heal
# the cluster will always be able to continue.
#
# cluster-slave-validity-factor 10

# 集群slave可以迁移到孤儿master那里，孤儿master没有工作的slave。
# 这提高了集群抵抗故障的能力，否则，如果没有工作的slave，则无法在故障发生时进行故障转移
# Cluster slaves are able to migrate to orphaned masters, that are masters
# that are left without working slaves. This improves the cluster ability
# to resist to failures as otherwise an orphaned master can't be failed over
# in case of failure if it has no working slaves.
#
# 只有在至少还有一个孤儿的情况下，slave才会迁移到孤儿master那里
#考虑到他们老 master的其他工作slave的数量。这个数字就是“移民障碍”。迁移屏障为1意味着，
#只有在至少有1个其他工作的slave为其主机工作时，slave才会迁移，以此类推。它通常反映集群中每个主服务器所需的从服务器数量
# Slaves migrate to orphaned masters only if there are still at least a
# given number of other working slaves for their old master. This number
# is the "migration barrier". A migration barrier of 1 means that a slave
# will migrate only if there is at least 1 other working slave for its master
# and so forth. It usually reflects the number of slaves you want for every
# master in your cluster.
#
# 默认值为1（仅当其主服务器与至少一个从服务器保持在一起时，从服务器才会迁移）。要禁用迁移，只需将其设置为一个非常大的值。
# 可以设置0值，但仅在调试时有用，在生产中很危险
# Default is 1 (slaves migrate only if their masters remain with at least
# one slave). To disable migration just set it to a very large value.
# A value of 0 can be set but is useful only for debugging and dangerous
# in production.
#
# cluster-migration-barrier 1

# 默认情况下，如果Redis集群节点检测到至少有一个未覆盖的哈希槽（没有可用的节点正在服务它），则停止接受查询。
# 这样，如果集群部分关闭（例如，不再覆盖某个范围的散列槽），整个集群最终将不可用。一旦所有槽位再次被覆盖，它就会自动返回可用
# By default Redis Cluster nodes stop accepting queries if they detect there
# is at least an hash slot uncovered (no available node is serving it).
# This way if the cluster is partially down (for example a range of hash slots
# are no longer covered) all the cluster becomes, eventually, unavailable.
# It automatically returns available as soon as all the slots are covered again.
#
# 但是，有时您希望正在工作的集群子集继续接受对仍然覆盖的键空间部分的查询。为此，只需将cluster-require-full-coverage选项设置为no
# However sometimes you want the subset of the cluster which is working,
# to continue to accept queries for the part of the key space that is still
# covered. In order to do so, just set the cluster-require-full-coverage
# option to no.
#
# cluster-require-full-coverage yes

# In order to setup your cluster make sure to read the documentation
# available at http://redis.io web site.

### 慢日志
################################## SLOW LOG ###################################

# Redis Slow Log是一个记录超过指定执行时间的查询的系统
# 执行时间不包括I/O操作，如与客户端对话、发送回复等，
# 而只是实际执行命令所需的时间（这是命令执行的唯一阶段，线程被阻塞，同时不能服务其他请求）。
# The Redis Slow Log is a system to log queries that exceeded a specified
# execution time. The execution time does not include the I/O operations
# like talking with the client, sending the reply and so forth,
# but just the time needed to actually execute the command (this is the only
# stage of command execution where the thread is blocked and can not serve
# other requests in the meantime).
#

# 你可以用两个参数配置慢速日志：
# 一个参数告诉Redis执行时间（以微秒为单位），命令需要超过多长时间才能被记录，
# 另一个参数是慢速日志的长度。记录新命令时，将从记录命令队列中删除最老的命令
# You can configure the slow log with two parameters: one tells Redis
# what is the execution time, in microseconds, to exceed in order for the
# command to get logged, and the other parameter is the length of the
# slow log. When a new command is logged the oldest one is removed from the
# queue of logged commands.

# 下面的时间以微秒表示，所以1000000相当于一秒。请注意，负数禁用慢速日志，而0的值强制记录每个命令
# The following time is expressed in microseconds, so 1000000 is equivalent
# to one second. Note that a negative number disables the slow log, while
# a value of zero forces the logging of every command.
slowlog-log-slower-than 10000

# 这个长度没有限制。只是要注意它会消耗内存。您可以使用SLOWLOG RESET来回收慢日志所使用的内存。
# There is no limit to this length. Just be aware that it will consume memory.
# You can reclaim memory used by the slow log with SLOWLOG RESET.
slowlog-max-len 128

################################ LATENCY MONITOR ##############################

# Redis延迟监控子系统在运行时对不同的操作进行采样，以便收集与Redis实例可能的延迟来源相关的数据
# The Redis latency monitoring subsystem samples different operations
# at runtime in order to collect data related to possible sources of
# latency of a Redis instance.
#
# 通过使用LATENCY命令，用户可以使用这些信息来打印图形和获取报告
# Via the LATENCY command this information is available to the user that can
# print graphs and obtain reports.
#
# 日志文件中指定的时间等于或大于毫秒数的操作，系统才会记录操作日志
# 延迟监视器阈值配置指令。当其值设置为零时，将关闭延迟监视器。
# The system only logs operations that were performed in a time equal or
# greater than the amount of milliseconds specified via the
# latency-monitor-threshold configuration directive. When its value is set
# to zero, the latency monitor is turned off.
#
# 默认情况下，延迟监视是禁用的，因为如果没有延迟问题，通常不需要它，并且收集数据会对性能产生影响，虽然影响很小，但可以在大负载下进行测量。
# 如果需要，可以在运行时使用命令“CONFIG SET Latency -monitor-threshold <milliseconds>”轻松启用延迟监视
# By default latency monitoring is disabled since it is mostly not needed
# if you don't have latency issues, and collecting data has a performance
# impact, that while very small, can be measured under big load. Latency
# monitoring can easily be enabled at runtime using the command
# "CONFIG SET latency-monitor-threshold <milliseconds>" if needed.
latency-monitor-threshold 0

############################# EVENT NOTIFICATION ##############################

# Redis可以通知Pub/Sub客户端key空间中发生的事件。该特性的文档位于http://redis.io/topics/notifications
# Redis can notify Pub/Sub clients about events happening in the key space.
# This feature is documented at http://redis.io/topics/notifications
#
# 例如，如果启用了keyspace事件通知，并且客户机对存储在Database 0中的键“foo”执行DEL操作，则将通过Pub/Sub发布两条消息
# For instance if keyspace events notification is enabled, and a client
# performs a DEL operation on key "foo" stored in the Database 0, two
# messages will be published via Pub/Sub:
#
# PUBLISH __keyspace@0__:foo del
# PUBLISH __keyevent@0__:del foo
#
# 可以在一组类中选择Redis将通知的事件。每个类都由单个字符标识
# It is possible to select the events that Redis will notify among a set
# of classes. Every class is identified by a single character:
#
#  K     Keyspace events, published with __keyspace@<db>__ prefix.
#  E     Keyevent events, published with __keyevent@<db>__ prefix.
#  g     Generic commands (non-type specific) like DEL, EXPIRE, RENAME, ...
#  $     String commands
#  l     List commands
#  s     Set commands
#  h     Hash commands
#  z     Sorted set commands
#  x     Expired events (events generated every time a key expires)
#  e     Evicted events (events generated when a key is evicted for maxmemory)
#  A     Alias for g$lshzxe, so that the "AKE" string means all the events.
#
# “notify-keyspace-events”的参数是一个由零个或多个字符组成的字符串。空字符串表示禁用通知
#  The "notify-keyspace-events" takes as argument a string that is composed
#  of zero or multiple characters. The empty string means that notifications
#  are disabled.
#
#  若要从事件名称的角度启用列表事件和一般事件，请使用
#  Example: to enable list and generic events, from the point of view of the
#           event name, use:
#
#  notify-keyspace-events Elg
#
#  订阅通道名称__keyevent@0__:expired  获取过期密钥流
#  Example 2: to get the stream of the expired keys subscribing to channel
#             name __keyevent@0__:expired use:
#
#  notify-keyspace-events Ex
#
# 默认情况下，所有通知都是禁用的，因为大多数用户不需要这个功能，
# 而且这个功能有一些开销。请注意，如果您没有指定K或E中的至少一个，则不会交付任何事件
#  By default all notifications are disabled because most users don't need
#  this feature and the feature has some overhead. Note that if you don't
#  specify at least one of K or E, no events will be delivered.
notify-keyspace-events ""

###高级配置
############################### ADVANCED CONFIG ###############################
# 当has只有少数条目时使用一个内存紧凑的数据结构， 最大条目没有给定一个阈值。
# 这些阈值可以通过以下指令配置。
# Hashes are encoded using a memory efficient data structure when they have a
# small number of entries, and the biggest entry does not exceed a given
# threshold. These thresholds can be configured using the following directives.
hash-max-ziplist-entries 512
hash-max-ziplist-value 64


# 列表也以一种特殊的方式编码，以节省大量的空间
# 每个内部列表节点允许的条目数可以指定为固定的最大大小或最大元素数
# 对于固定的最大大小，使用-5到-1，表示

# 正数表示每个列表节点存储的元素数量不超过这个数目

# 最高性能的选项通常是-2 （8 Kb大小）或-1 （4 Kb大小），但如果您的用例是唯一的，请根据需要调整设置
# Lists are also encoded in a special way to save a lot of space.
# The number of entries allowed per internal list node can be specified
# as a fixed maximum size or a maximum number of elements.
# For a fixed maximum size, use -5 through -1, meaning:
# -5: max size: 64 Kb  <-- not recommended for normal workloads
# -4: max size: 32 Kb  <-- not recommended
# -3: max size: 16 Kb  <-- probably not recommended
# -2: max size: 8 Kb   <-- good
# -1: max size: 4 Kb   <-- good
# Positive numbers mean store up to _exactly_ that number of elements
# per list node.
# The highest performing option is usually -2 (8 Kb size) or -1 (4 Kb size),
# but if your use case is unique, adjust the settings as necessary.
list-max-ziplist-size -2

# 当列表很长的时候，最容易被访问的很可能是两端的数据，中间的数据被访问的频率比较低（访问起来性能也很低）。如果应用场景符合这个特点，
# 那么list还提供了一个选项，能够把中间的数据节点进行压缩，从而进一步节省内存空间。Redis的配置参数list-compress-depth就是用来完成这个设置的


# Lists may also be compressed.
# Compress depth is the number of quicklist ziplist nodes from *each* side of
# the list to *exclude* from compression.  The head and tail of the list
# are always uncompressed for fast push/pop operations.  Settings are:
# 0: disable all list compression
# 1: depth 1 means "don't start compressing until after 1 node into the list,
#    going from either the head or tail"
#    So: [head]->node->node->...->node->[tail]
#    [head], [tail] will always be uncompressed; inner nodes will compress.
# 2: [head]->[next]->node->node->...->node->[prev]->[tail]
#    2 here means: don't compress head or head->next or tail->prev or tail,
#    but compress all nodes between them.
# 3: [head]->[next]->[next]->node->node->...->node->[prev]->[prev]->[tail]
# etc.
list-compress-depth 0

# 只有在一种情况下，集合具有特殊的编码：
# 当集合恰好由64位带符号整数范围内以10为基数的整数组成时。
#下面的配置设置设置了集合大小的限制，以便使用这种特殊的内存保存编码
# Sets have a special encoding in just one case: when a set is composed
# of just strings that happen to be integers in radix 10 in the range
# of 64 bit signed integers.
# The following configuration setting sets the limit in the size of the
# set in order to use this special memory saving encoding.
set-max-intset-entries 512

# 与散列和列表类似，排序集也经过特殊编码，以节省大量空间。此编码仅在排序集的长度和元素低于以下限制时使用
# Similarly to hashes and lists, sorted sets are also specially encoded in
# order to save a lot of space. This encoding is only used when the length and
# elements of a sorted set are below the following limits:
zset-max-ziplist-entries 128
zset-max-ziplist-value 64


# HyperLogLog稀疏表示字节限制。该限制包括16字节的报头。当使用稀疏表示的HyperLogLog超过此限制时，它将被转换为密集表示。大于16000的值是完全无用的，因为在这个点上，密集的表示更有效地利用内存。
#  建议的值为~ 3000，以便在不降低太多PFADD速度的情况下获得空间高效编码的好处，在稀疏编码下，PFADD的速度为0 (N)
#  当不考虑CPU，但考虑空间，并且数据集由许多基数在0 - 15000范围内的hyperloglog组成时，该值可以提高到~ 10000
# HyperLogLog sparse representation bytes limit. The limit includes the
# 16 bytes header. When an HyperLogLog using the sparse representation crosses
# this limit, it is converted into the dense representation.
#
# A value greater than 16000 is totally useless, since at that point the
# dense representation is more memory efficient.
#
# The suggested value is ~ 3000 in order to have the benefits of
# the space efficient encoding without slowing down too much PFADD,
# which is O(N) with the sparse encoding. The value can be raised to
# ~ 10000 when CPU is not a concern, but space is, and the data set is
# composed of many HyperLogLogs with cardinality in the 0 - 15000 range.
hll-sparse-max-bytes 3000

# 主动重哈希每100毫秒使用1毫秒的CPU时间，以帮助重哈希主Redis哈希表（映射顶级键到值的表）。
# Redis使用的哈希表实现（参见dict.c）执行惰性重哈希：
# 您运行到重哈希的哈希表中的操作越多，执行的重哈希“步骤”就越多，
# 因此，如果服务器空闲，重哈希永远不会完成，并且哈希表会使用更多内存
# Active rehashing uses 1 millisecond every 100 milliseconds of CPU time in
# order to help rehashing the main Redis hash table (the one mapping top-level
# keys to values). The hash table implementation Redis uses (see dict.c)
# performs a lazy rehashing: the more operation you run into a hash table
# that is rehashing, the more rehashing "steps" are performed, so if the
# server is idle the rehashing is never complete and some more memory is used
# by the hash table.
#
# The default is to use this millisecond 10 times every second in order to
# actively rehash the main dictionaries, freeing memory when possible.
#
# If unsure:
# use "activerehashing no" if you have hard latency requirements and it is
# not a good thing in your environment that Redis can reply from time to time
# to queries with 2 milliseconds delay.
#
# use "activerehashing yes" if you don't have such hard requirements but
# want to free memory asap when possible.
activerehashing yes

# 客户端输出缓冲区限制可用于强制断开由于某些原因而没有足够快地从服务器读取数据的客户端
# （常见的原因是Pub/Sub客户端不能像发布者生成消息那样快地使用消息）。
# The client output buffer limits can be used to force disconnection of clients
# that are not reading data from the server fast enough for some reason (a
# common reason is that a Pub/Sub client can't consume messages as fast as the
# publisher can produce them).
#
# 对于三种不同类别的客户，可以设置不同的限制
# The limit can be set differently for the three different classes of clients:
#
# normal -> normal clients including MONITOR clients 正常客户端包括MONITOR客户端
# slave  -> slave clients 从服务客户端
# pubsub -> clients subscribed to at least one pubsub channel or pattern 至少订阅一个频道的客户端
#
# 每个client-output-buffer-limit指令的语法如下
# The syntax of every client-output-buffer-limit directive is the following:
#
# client-output-buffer-limit <class> <hard limit> <soft limit> <soft seconds>
#
# 一旦达到硬限制，或者达到软限制并在指定的秒数（连续）内保持达到，客户机将立即断开连接。
# A client is immediately disconnected once the hard limit is reached, or if
# the soft limit is reached and remains reached for the specified number of
# seconds (continuously).
# 例如，如果硬限制是32兆字节，软限制是16兆字节/ 10秒，
# 如果输出缓冲区的大小达到32兆字节，客户端将立即断开连接，但如果客户端达到16兆字节并连续超过限制10秒，也会断开连接
# So for instance if the hard limit is 32 megabytes and the soft limit is
# 16 megabytes / 10 seconds, the client will get disconnected immediately
# if the size of the output buffers reach 32 megabytes, but will also get
# disconnected if the client reaches 16 megabytes and continuously overcomes
# the limit for 10 seconds.
#
# 默认情况下，普通客户端不受限制，因为它们不会在没有请求的情况下接收数据（以推送方式），
# 而是在请求之后接收数据，因此只有异步客户端可能会创建请求数据比读取数据快的场景
# By default normal clients are not limited because they don't receive data
# without asking (in a push way), but just after a request, so only
# asynchronous clients may create a scenario where data is requested faster
# than it can read.
#
# 相反，对于公共订阅和从属客户端有一个默认限制，因为
# 订阅者和从服务器以推送方式接收数据
# Instead there is a default limit for pubsub and slave clients, since
# subscribers and slaves receive data in a push fashion.
#
# Both the hard or the soft limit can be disabled by setting them to zero.
client-output-buffer-limit normal 0 0 0
client-output-buffer-limit slave 256mb 64mb 60
client-output-buffer-limit pubsub 32mb 8mb 60

# Redis调用一个内部函数去执行很多后台任务，像
# 关闭超时的客户端连接，清除从未请求过的过期键，依此类推

# 不是所有的任务都以相同的频率执行，但是Redis根据指定的“hz”值检查要执行的任务

# "hz"默认值被设置为10.当Redis空闲时，提高该值将使用更多的CPU，
# 但同时也会使Redis在同时有很多键到期时响应更快，并且可以更精确地处理超时。
# 范围在1到500之间，但是超过100的值通常不是一个好主意，
# 大多数用户应该使用默认值10，只有在需要非常低延迟的环境中才将其提高到100
# Redis calls an internal function to perform many background tasks, like
# closing connections of clients in timeout, purging expired keys that are
# never requested, and so forth.
#
# Not all tasks are performed with the same frequency, but Redis checks for
# tasks to perform according to the specified "hz" value.
#
# By default "hz" is set to 10. Raising the value will use more CPU when
# Redis is idle, but at the same time will make Redis more responsive when
# there are many keys expiring at the same time, and timeouts may be
# handled with more precision.
#
# The range is between 1 and 500, however a value over 100 is usually not
# a good idea. Most users should use the default of 10 and raise this up to
# 100 only in environments where very low latency is required.
hz 10

# 当子节点重写AOF文件时，如果启用了以下选项，则文件将每生成32mb的数据进行f同步。
# 这对于以增量方式将文件提交到磁盘并避免大的延迟峰值非常有用
# When a child rewrites the AOF file, if the following option is enabled
# the file will be fsync-ed every 32 MB of data generated. This is useful
# in order to commit the file to the disk more incrementally and avoid
# big latency spikes.
aof-rewrite-incremental-fsync yes
